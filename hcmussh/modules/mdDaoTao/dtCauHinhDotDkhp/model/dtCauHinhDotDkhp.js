// eslint-disable-next-line no-unused-vars
module.exports = app => {
    // app.model.dtCauHinhDotDkhp.foo = () => { };
    // app.model.dtCauHinhDotDkhp.getAllItems = async () => {
    //     const [listDot] = await Promise.all([
    //         app.model.dtCauHinhDkhp.getAll(),
    //     ]);
    //     const listId = listDot.map(dot => dot.id);
    //     if (listId.length == 0) return [];

    //     let items = await app.model.dtCauHinhDotDkhp.getAll({ statement: 'idDot IN (:listId)', parameter: { listId } }, '*', 'id');
    //     const result = listDot.map(dot => ({
    //         ...dot,
    //         submenus: items.filter(item => item.idDot == dot.id),
    //     }));

    //     return result;
    // };
};