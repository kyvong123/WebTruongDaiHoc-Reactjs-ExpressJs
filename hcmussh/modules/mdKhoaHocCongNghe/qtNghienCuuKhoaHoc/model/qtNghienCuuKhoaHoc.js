// eslint-disable-next-line no-unused-vars
module.exports = app => {
    // app.model.qtNghienCuuKhoaHoc.foo = () => { };
    // app.model.qtNghienCuuKhoaHoc.searchPage = (pagenumber, pagesize, searchterm, mscb, filter, done) => {
    //     app.database.oracle.connection.main.executeExtra('BEGIN :ret:=qt_nckh_search_page(:pagenumber, :pagesize, :searchterm, :totalitem, :pagetotal, :mscb, :filter); END;',
    //         { ret: { dir: app.database.oracle.BIND_OUT, type: app.database.oracle.CURSOR }, pagenumber: { val: pagenumber, dir: app.database.oracle.BIND_INOUT, type: app.database.oracle.NUMBER }, pagesize: { val: pagesize, dir: app.database.oracle.BIND_INOUT, type: app.database.oracle.NUMBER }, searchterm, totalitem: { dir: app.database.oracle.BIND_OUT, type: app.database.oracle.NUMBER }, pagetotal: { dir: app.database.oracle.BIND_OUT, type: app.database.oracle.NUMBER }, mscb, filter }, (error, result) => app.database.oracle.fetchRowsFromCursor(error, result, done));
    // };

    // app.model.qtNghienCuuKhoaHoc.downloadExcel = (filter, done) => {
    //     app.database.oracle.connection.main.executeExtra('BEGIN :ret:=qt_nckh_download_excel(:filter); END;',
    //         { ret: { dir: app.database.oracle.BIND_OUT, type: app.database.oracle.CURSOR }, filter }, (error, result) => app.database.oracle.fetchRowsFromCursor(error, result, done));
    // };

    // app.model.qtNghienCuuKhoaHoc.groupPage = (pagenumber, pagesize, searchterm, mscb, filter, done) => {
    //     app.database.oracle.connection.main.executeExtra('BEGIN :ret:=qt_nckh_group_page(:pagenumber, :pagesize, :searchterm, :totalitem, :pagetotal, :mscb, :filter); END;',
    //         { ret: { dir: app.database.oracle.BIND_OUT, type: app.database.oracle.CURSOR }, pagenumber: { val: pagenumber, dir: app.database.oracle.BIND_INOUT, type: app.database.oracle.NUMBER }, pagesize: { val: pagesize, dir: app.database.oracle.BIND_INOUT, type: app.database.oracle.NUMBER }, searchterm, totalitem: { dir: app.database.oracle.BIND_OUT, type: app.database.oracle.NUMBER }, pagetotal: { dir: app.database.oracle.BIND_OUT, type: app.database.oracle.NUMBER }, mscb, filter }, (error, result) => app.database.oracle.fetchRowsFromCursor(error, result, done));
    // };

    // app.model.qtNghienCuuKhoaHoc.userPage = (staffEmail, done) => {
    //     app.database.oracle.connection.main.executeExtra('BEGIN :ret:=qt_nghien_cuu_khoa_hoc_user_page(:staffEmail); END;',
    //         { ret: { dir: app.database.oracle.BIND_OUT, type: app.database.oracle.CURSOR }, staffEmail }, (error, result) => app.database.oracle.fetchRowsFromCursor(error, result, done));
    // };
};